const db = require('../model/mongo')
const request = require('request');
module.exports = {
    updateUserPresence: async(latestUpdate) => {
        console.log("Update user presence thread is running")
    }
}