//the interface for all players
//instantiate and assign handlers, or inherit

export default class UniversalVideoPlayer {
    constructor(
        init,
        setContentToPlay,
        getNowPlaying,
        play,
        stop,
        getPosition,
        seekToPosition,
        mute,
        unmute,
        getMuteState,
        getContentInfo,
        customUI) {
        }
}